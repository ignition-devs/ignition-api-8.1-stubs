name: main

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

jobs:
  test:
    uses: thecesrom/workflows/.github/workflows/tox-envs.yml@v1.1.1
    with:
      python-versions: '["3.7", "3.8", "3.9", "3.10"]'
