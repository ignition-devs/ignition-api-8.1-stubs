name: pr-build

on:
  pull_request:
    branches:
      - main
    paths:
      - 'stubs/**'
      - pyproject.toml
      - requirements.txt
      - tox.ini

jobs:
  test:
    uses: coatl-dev/workflows/.github/workflows/tox-envs.yml@v4
    with:
      python-versions: '["3.7", "3.8", "3.9", "3.10", "3.11", "3.12"]'
