[build-system]
build-backend = "setuptools.build_meta"
requires = [
  "setuptools>=59",
]

[tool.commitizen]
name = "cz_conventional_commits"

[tool.isort]
extra_standard_library = ["typing"]
profile = "black"
py_version = 27

[tool.mypy]
python_version = 2.7
